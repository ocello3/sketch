import { s } from './sketch.js';
import { setParams } from './params.js';

export const setDot = (index) => (params) => {
	const initDot = {};
	return initDot;
}

export const updateDot = (preDot, index) => (params) => {
	const newDot =  { ...preDot };
	return newDot;
}
